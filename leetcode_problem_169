class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        c=0
        candiate=0
        for num in nums:
            if c==0:
                candiate=num
                c+=1
            elif candiate==num:
                    c+=1
            else:
                 c-=1
        return candiate
        
